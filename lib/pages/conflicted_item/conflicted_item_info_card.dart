import 'package:flutter/cupertino.dart';

class ConflictedItemInfoCard extends StatelessWidget {
  const ConflictedItemInfoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Text("conflicted item info card"),
      ],
    );
  }
}
